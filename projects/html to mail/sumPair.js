// Given an unsorted of distinct integers, find the largest pair sum in it. For example,
// the largest pair sum in {12, 34, 10, 6, 40} is 74 in O(N) time-complexity.


// 


function searchel(array) {
    let element = 0;
    for (let i = 0; i < array.length; i++) {
        if (result == element) {
            return true;
        } else {
            return false;
        }
    }
}

const output = searchel([12, 34, 10, 6, 40]);
console.log(output);